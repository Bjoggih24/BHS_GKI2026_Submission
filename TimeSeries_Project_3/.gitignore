################################################################################
# PYTHON & ENVIRONMENT
################################################################################

# Byte-compiled / optimized / DLL files
__pycache__/
*.pyc
*.pyo
*.pyd

# Python distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/

# Virtual environments
venv/
env/
ENV/
.venv

# IDE and Editor files
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Python environment / dependency files
*.env
*.lock
.python-version

################################################################################
# PROJECT-SPECIFIC: DATA & ARTIFACTS
################################################################################

# Raw data files (zips + extracted CSVs are kept in repo for reproducibility)
# Generated by: unzip data/*.zip

# Extracted/intermediate data arrays (large, not tracked)
# Generated by: python scripts/build_train_full.py, build_val_set.py, etc.
data/*.npz
data/*.npy
data/train_batches/
data/weather_agg.csv
data/weather_obs_agg.csv
data/sensor_timeseries.csv
data/weather_forecasts.csv
data/weather_observations.csv





# Weather cache files (large, can be regenerated)
data/weather_cache/
data/*_agg.npz

# Trained model files (very large, not tracked)
# Generated by: python scripts/train_models.py
# Each run creates: artifacts/runs/<timestamp>_<model_name>/model.joblib (or .pkl)
artifacts/runs/**/model.joblib
artifacts/runs/**/model.pkl
artifacts/runs/**/*.joblib
artifacts/runs/**/*.pkl
artifacts/runs/**/config.json

# Prediction/validation outputs (can be regenerated)
artifacts/runs/**/predictions.npy
artifacts/runs/**/val_probs.npy
artifacts/runs/**/metrics.json

# Temporary logs and debugging outputs
*.log
*.out
*.err
logs/

# OS-specific
################################################################################
# OS-SPECIFIC
################################################################################

.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
desktop.ini

################################################################################
# SUBMISSION & INFERENCE (SHOULD BE TRACKED IN GIT)
################################################################################

# These files ARE important and SHOULD be committed:
# - Model configuration files: artifacts/active_model.json, submission_configs/*.json
# - API code: api.py, model.py, models_v2.py
# - Training scripts: scripts/build_train_full.py, scripts/build_weather_agg.py, scripts/train_models.py
# - Feature utilities: feature_utils.py, utils.py
# 
# Example git commands to track these:
# git add artifacts/active_model.json submission_configs/*.json
# git add api.py model.py models_v2.py
# git add scripts/build_train_full.py scripts/build_weather_agg.py scripts/train_models.py
# git add feature_utils.py utils.py

################################################################################
# PROJECT SCRIPTS (ONLY KEEP REPRO PIPELINE)
################################################################################

# Ignore all scripts by default, then allow the reproducible pipeline scripts.
scripts/*
!scripts/
!scripts/build_train_full.py
!scripts/build_weather_agg.py
!scripts/train_models.py
!scripts/eval_quick.py
!scripts/eval_api.py
!scripts/merge_batches.py