################################################################################
# PYTHON & ENVIRONMENT
################################################################################

# Byte-compiled / optimized / DLL files
__pycache__/
*.pyc
*.pyo
*.pyd

# Python distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/

# Virtual environments
venv/
env/
ENV/
.venv

# IDE and Editor files
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Python environment / dependency files
*.env
*.lock
.python-version

################################################################################
# PROJECT-SPECIFIC: DATA & ARTIFACTS
################################################################################

# Training data (kept in repo for reproducibility)

# Legacy config folder (submission configs live in submission_configs/)
configs/

# Precomputed features (large numpy arrays, not tracked in repo)
# Generated by: python scripts/precompute_tabular_features.py
artifacts/features/

# Experiment outputs and trained models (very large, not tracked)
# Generated during training runs (scripts/train_tabular.py, scripts/train_cnn.py, etc.)
# These are intermediate outputs; final models referenced in ensemble config
artifacts/experiments/

# Binary model artifacts (too large for version control)
# Includes: *.joblib (scikit-learn), *.pt (PyTorch), *.pth files
*.joblib
*.pt
*.pth

# Numpy/data arrays (unless critical for repo)
*.npy
*.npz

# Log files from training runs
*.log
*.out
*.err

################################################################################
# NON-SUBMISSION SCRIPTS (OPTIONAL/EXPERIMENTAL)
################################################################################

# These are optional/legacy helpers and not needed for the final submission
scripts/compute_norm_stats.py
scripts/ensemble_from_probs.py
scripts/ensemble_val.py
scripts/eval_local.py
scripts/eval_with_tta.py
scripts/extract_img_embeddings.py
scripts/merge_patches.py
scripts/run_tabular_batch.py
scripts/run_tabular_overnight.py
scripts/run_overnight_v3_mix.py

################################################################################
# SUBMISSION & INFERENCE (SHOULD BE TRACKED IN GIT)
################################################################################

# These files ARE important and SHOULD be committed:
# - Configuration templates: configs/
# - Core training scripts: scripts/train_*.py, scripts/eval_*.py
# - API and model loading code: api.py, model.py
# - Ensemble configuration for submission: cnn/ensemble_submit.json

################################################################################
# OS-SPECIFIC
################################################################################

.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
desktop.ini
